/*
package com.example.courier.adapter.out.persistence.order.projection.deletelater;

import projection.parcel.persistence.out.adapter.gytis.courier.ParcelProjection;
import parcel.readmodel.application.gytis.courier.ParcelReadModel;
import org.springframework.stereotype.Component;

@Component
public class ParcelReadModelMapper {
    public ParcelReadModel toParcel(ParcelProjection projection) {
        return new ParcelReadModel(
                projection.getId(),
                projection.getContents(),
                projection.getWeight().getName(),
                projection.getDimensions().getName(),
                projection.getTrackingNumber(),
                projection.getStatus()
        );
    }
}
*/
