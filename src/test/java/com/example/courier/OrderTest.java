package com.example.courier;
/*
import com.example.courier.domain.Order;
import com.example.courier.domain.Package;
import com.example.courier.domain.User;
import org.junit.jupiter.api.Test;

import java.time.LocalDateTime;

import static org.junit.jupiter.api.Assertions.*;

public class OrderTest {

    @Test
    void testCreateOrder_shouldReturnValidOrder() {
        //arrange
        User user = new User("Bilbo Baggins", "bilbo@middleearth.com", "Bag End", "underhill99");
        Package packageDetails = new Package(2.5, "10x10x10", "Books", "abc123", "Pending");
        LocalDateTime createdDate = LocalDateTime.now();
        Order order = new Order(user, "Bag End", "Bagshot Row", packageDetails, "Express", "Pending", createdDate);

        // assert
        assertEquals(user, order.getUser());
        assertEquals("Bag End", order.getSenderAddress());
        assertEquals("Bagshot Row", order.getRecipientAddress());
        assertEquals(packageDetails, order.getPackageDetails());
        assertEquals("Express", order.getDeliveryPreferences());
        assertEquals("Pending", order.getStatus());
        assertEquals(createdDate, order.getCreateDate());
    }

}

 */